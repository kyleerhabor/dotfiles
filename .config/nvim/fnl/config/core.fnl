(local highlight-duration 500)

;(local colorscheme
;  (if (= vim.env.TERM_PROGRAM terms.apple)
;    "gruvbox-material"
;    ;; Despite what the README says, oxocarbon requires termguicolors to function well.
;    "oxocarbon"))

{"highlight-duration" highlight-duration}
