(local auto (require "config.auto"))
(local {: set-colorscheme} (require "config.colorscheme"))

;; (auto.command "User" (auto.group "LumenLight" {"clear" false})
;;   {"callback" set-colorscheme})
;;
;; (auto.command "User" (auto.group "LumenDark" {"clear" false})
;;   {"callback" set-colorscheme})
